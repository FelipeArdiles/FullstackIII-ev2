server:
  port: 8081
spring:
  application:
    name: ${artifactId}
  datasource:
    url: jdbc:h2:mem:${artifactId};DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
