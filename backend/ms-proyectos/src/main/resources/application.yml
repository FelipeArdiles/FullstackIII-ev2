server:
  port: 8081
spring:
  application:
    name: ms-proyectos
  datasource:
    url: jdbc:h2:mem:proyectos;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  h2:
    console:
      enabled: true
