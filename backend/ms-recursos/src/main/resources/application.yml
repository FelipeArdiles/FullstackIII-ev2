server:
  port: 8082
spring:
  application:
    name: ms-recursos
  datasource:
    url: jdbc:h2:mem:recursos;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
services:
  proyectos:
    url: http://localhost:8081
